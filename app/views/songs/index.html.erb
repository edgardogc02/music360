<%= render 'shared/header_page', title: 'Songs' %>

<% if @searched_songs and !@searched_songs.songs.blank? %>
  <%= render "songs_list", songs_list: @searched_songs %>
<% elsif @searched_songs and @searched_songs.songs.empty? %>
  <div class="alert alert-warning">
    No results were found
  </div>
<% end %>

<% if @songs and !@songs.songs.blank? %>
	<%= render "songs_list", songs_list: @songs %>
<% end %>

<% if @my_songs and !@my_songs.songs.blank? %>
	<%= render "songs_list", songs_list: @my_songs %>
<% end %>

<% if @most_popular_songs %>
  <%= render "songs_list", songs_list: @most_popular_songs %>
<% end %>

<% if @new_songs %>
  <%= render "songs_list", songs_list: @new_songs %>
<% end %>
