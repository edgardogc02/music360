<%= render 'shared/header_page', title: 'Songs' %>

Difficulty:
<%= link_to 'All', params.merge({difficulty: 'all'}), {class: 'btn btn-primary'} %>
<%= link_to 'Easy', params.merge({difficulty: 'easy'}), {class: 'btn btn-primary'} %>
<%= link_to 'Medium', params.merge({difficulty: 'medium'}), {class: 'btn btn-primary'} %>
<%= link_to 'Hard', params.merge({difficulty: 'hard'}), {class: 'btn btn-primary'} %>

<%= render "songs_list", songs_list: @songs %>