<%= simple_form_for current_user.inverse_user_followers.build(user_id: user.id), remote: true, url: user_groupies_path, html: {id: "new_user_follower_#{user.id}"} do |f| %>
  <%= f.input :user_id, as: :hidden %>
<% end %>

<%= link_to "Follow", "#", {class: "follow_user btn btn-sm btn-default", id: "follow_user_#{user.id}"} %>

