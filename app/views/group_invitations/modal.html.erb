<%= render 'shared/header_page', title: 'Invite members' %>

<%= form_tag({action: "via_email", method: :post}) do %>
  <%= label_tag :email_group_invitation, "Invite a friend via email" %>
  <%= text_field_tag :email_group_invitation %>
  <%= submit_tag "Invite", {id: "group_invitation_via_email_submit"} %>
  <%= %>
<% end %>

<hr />

<%= render 'users/search_form', action: "index", controller: "group_invitations" %>
<br />

<%= render "users/users_list", users_list: @fb_top_friends, modal: true %>

<%= render "users/users_list", users_list: @regular_users, modal: true %>

<script type="text/javascript">
  $(document).ready(listing_hover_effects);
  $(document).on('page:load', listing_hover_effects);
</script>

