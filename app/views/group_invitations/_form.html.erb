<%= simple_form_for [@group, GroupInvitation.new], html: { id: "new_group_invitation_#{user_id}" } do |f| %>

  <%= f.input :user_id, as: :hidden, input_html: { value: user_id } %>
  <%= f.submit "Invite", {class: "btn btn-primary", id: "submit_group_invitation_#{user_id}"} %>

<% end %>
