<h1 class="page-header">Instruments</h1>

<% @instruments.each do |instrument| %>
  <div>
    <%= image_tag instrument.image_url, class: "thumbnail" %>
    <%= instrument.name %>
    <%= link_to "Edit", edit_instrument_path(instrument) %>
  </div>
  <hr />
<% end %>
