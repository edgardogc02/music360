<h1>Connect with other music players</h1>
<br />
<p class="description">
  Playing music is more fun when playing together.
  Connect with other music players in the community,
  you can follow and unfollow them later.
</p>
<br />

<%= form_tag create_multiple_user_followers_path do %>

  <% @popular_players.each do |player| %>
    <%= hidden_field_tag :user_ids, player.id_user, {name: "follow_ids[]", id: "follow_player_#{player.id_user}"} %>
  <% end %>

  <%= submit_tag "Continue", class: "btn btn-primary btn-lg btn-full-width" %>
  <div class="pull-right">
    <%= link_to "skip", getting_started_music_challenges_path, class: "btn btn-link btn-lg" %>
  </div>

<% end %>

<br /><br />

<h3>Popular music players</h3>

<div class="group-challenge-item">
  <% @popular_players.each do |player| %>
    <div class="text-left clearfix follow-player item-content">
      <%= image_tag player.imagename, class: "avatar-sm pull-left margin-right" %>
      <h4><%= player.username %></h4>
      Career level: <%= player.level %>
      <%= link_to "#", {class: "btn btn-default btn-xs pull-right remove-btn remove-follow-player", data: {id: player.id_user}} do %>
        <%= content_tag :i, nil, class: "glyphicon glyphicon-remove" %>
      <% end %>
    </div>
  <% end %>
</div>