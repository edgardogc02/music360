<%= content_tag :div, {id: "unfollow_#{user.id}" } do %>
  <%= simple_form_for current_user.inverse_user_followers.find_by(user_id: user.id), html: {remote: true, id: "new_user_follower_#{user.id}", method: :delete} do |f| %>
  <% end %>

  <%= link_to "Following", "#", {class: "unfollow_user btn btn-primary " + css_classes} %>
<% end %>
