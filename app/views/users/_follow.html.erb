<%= simple_form_for current_user.inverse_user_followers.build(user_id: user.id), html: {id: "new_user_follower_#{user.id}"} do |f| %>
  <%= f.input :user_id, as: :hidden %>
<% end %>

<%= link_to "Follow", "#", {class: "follow_user btn btn-primary " + size, id: "follow_user_#{user.id}"} %>

