$('#more_posts').prepend("<%= j render @activities %>");
<% unless @activities.last_page? %>
  $('#show_more_posts_link').attr('href', "<%= group_group_activities_path(@group, page: params[:page].to_i+1) %>");
<% else %>
  $('#show_more_posts_link').remove();
<% end %>
