<%= render 'shared/header_page', title: 'Specify gift receiver' %>

<p>Do you know the username or the email of the gift receiver? Otherwise leave it blank.</p>

<div class="row">
  <div class="col-lg-6">
    <%= simple_form_for [@payment, @redeem_code] do |f| %>

      <%= f.input :gift_receiver %>
      <%= f.submit 'Generate voucher', {class: 'btn btn-primary'} %>

    <% end %>
  </div>
</div>